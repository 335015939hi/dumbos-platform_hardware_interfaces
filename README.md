You need to manually run update-base-files.sh after editing .hal files.
In addition, there are some .hal files that aren't processed by that script,
and so the corresponding .h have to be manually edited.  See bug 122858783.
